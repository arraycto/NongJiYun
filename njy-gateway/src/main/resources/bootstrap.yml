server:
  port: 8088
spring:
  application:
    name: njy-gateway
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        name: njy-gateway
      discovery:
        namespace: dev
        server-addr: 127.0.0.1:8848
management:
  endpoints:
    web:
      exposure:
        include: '*'